package net.cvs0.flare.tokens;

/**
 * Enum for supported types in the language.
 * Easily extensible for new types.
 */
public enum Type {
    INT, FLOAT, STRING, BOOL, FUNCTION, MODULE, VARIANT, TAG, ANY
}
